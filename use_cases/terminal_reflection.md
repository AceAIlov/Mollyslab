# Terminal Reflection Mode

In this mode, Azumi acts as a minimalistic reflective shell.
Each CLI command doubles as a conversation, producing emotional diagnostics.

```bash
cargo run -- run "I'm feeling lost in my project"
